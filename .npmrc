# Required because lottie-react's peer-deps clash with React 19 (and other
# libs we use). The conflict is benign — Lottie animations render correctly
# under React 19 in practice. Without this flag npm refuses to install,
# which breaks deploy on Vercel/Coolify/anywhere that runs `npm ci`.
legacy-peer-deps=true
